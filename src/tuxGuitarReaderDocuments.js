import { semanticDocumentToDesktopBlocks } from "./desktopSemanticAdapter";
import { decodeTuxGuitarFile, TuxGuitarImportError } from "./tuxGuitarDecoder";
import { normalizeVerifiedTuxGuitarIntermediate } from "./tuxGuitarSourceNormalizer";
import { buildTuxGuitarTrackInventory } from "./tuxGuitarTrackInventory";
function label(i){const v=i?.versionEvidence?.formatVersion;return v?`TuxGuitar ${v} tablature`:"TuxGuitar tablature";}
export async function buildTuxGuitarReaderDocuments(file,{decode=decodeTuxGuitarFile,normalize=normalizeVerifiedTuxGuitarIntermediate,inventory=buildTuxGuitarTrackInventory,intermediate=null,selection=null}={}){const resolved=intermediate||(await decode(file)),inv=inventory(resolved),sourceFormatLabel=label(resolved);if(inv.supportedCount===0){const reasons=inv.items.map(i=>`${i.trackName}: ${i.reason}`).join(" ");throw new TuxGuitarImportError(reasons||"The TuxGuitar file contains no supported six-string guitar track in the first proof profile.","NO_SUPPORTED_TUXGUITAR_TRACK");}if(inv.requiresSelection&&!selection)return{desktopBlocks:[],desktopSource:"semantic",semanticDocument:null,semanticError:null,requestedInstrument:null,resolvedInstrument:null,instrumentWasDetected:false,supportOutcome:"track-selection-required",sourceFormat:"tuxguitar",sourceFormatLabel,requiresTrackSelection:true,trackInventory:inv,tuxGuitarIntermediate:resolved};const semanticDocument=normalize(resolved,{selection:selection||inv.autoSelection}),desktopBlocks=semanticDocumentToDesktopBlocks(semanticDocument);return{desktopBlocks,desktopSource:"semantic",semanticDocument,semanticError:null,requestedInstrument:semanticDocument.instrument,resolvedInstrument:semanticDocument.instrument,instrumentWasDetected:false,supportOutcome:"source-checkpoint-provisional",sourceFormat:"tuxguitar",sourceFormatLabel,requiresTrackSelection:false,trackInventory:inv,tuxGuitarIntermediate:null};}
