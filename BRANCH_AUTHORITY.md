# Branch Authority — Clean Semantic Continuation

Date: August 3, 2026

Repository: `BlindAnatomist/guitar-eyes`

Branch: `work/clean-semantic-continuation`

## Exact ancestry

This documentation-only clean continuation branch was created directly from the hosted and real-iPhone-accepted application source:

`51741c03a9eaa339940c84d53e0f0f00e6413a93`

No sampled-audio branch, Iowa sample implementation, sampled manifest, WAV asset, extraction script, sample audit, sample-specific test, gain experiment, or temporary Iowa workflow is part of this branch's ancestry or permitted inheritance.

The accepted application source remains the application authority. This documentation-only commit does not replace it.

## Purpose

This branch exists to prevent future format, teacher-mode, semantic, reader, practice, or accessibility work from accidentally continuing the rejected sampled-playback experiment.

It is a clean planning and branching base only. It does not authorize a feature implementation by itself.

Before implementation begins, the owner must select one separately bounded checkpoint, such as:

1. additional tablature-format intake; or
2. teacher-mode foundation.

Do not combine those checkpoints in one assignment.

## Prohibited ancestry and inheritance

Future work must not begin from, merge from, rebase onto, or cherry-pick implementation from any of these experimental branches:

- `work/iowa-sample-audition-proof-1i`;
- `work/iowa-sample-loudness-proof-1j`;
- `work/iowa-sample-integrity-focus-proof-1k`;
- `work/iowa-sample-systemic-repair-1k`;
- any Iowa sample execution-gate or publication branch; or
- any descendant containing sampled-audio assets or sample-specific playback machinery.

The rejected sampled experiment is preserved only as evidence. Its closure authority is:

`work/iowa-sample-systemic-repair-1k/BRANCH_AUTHORITY.md`

## Accepted inherited system

This branch inherits only the accepted 1G application contracts, including:

1. one shared semantic tablature document;
2. one accepted deterministic playback timeline;
3. accepted ASCII guitar and bass intake;
4. accepted bounded uncompressed and compressed MusicXML intake;
5. accepted bounded project-authored Guitar Pro intake and explicit track selection;
6. converged iPhone and desktop readers;
7. Previous position, Read current position, Next position in that order;
8. Audition current position after the navigation group;
9. quiet movement and dedicated semantic speech;
10. the accepted project-owned procedural current-position sound;
11. the accepted two-second sound delay;
12. accepted first-audition focus behavior; and
13. safe rejection instead of guessed musical interpretation.

## Teacher-mode rule

A future teacher-mode checkpoint must consume the existing semantic document and accepted playback timeline. It must not create a second parser, second musical model, second duration model, or dependency on sampled playback.

Teacher mode may use speech, semantic position structure, repetition, questions, progressive disclosure, and accepted timing evidence without requiring realistic instrument samples.

## Format-expansion rule

A future format checkpoint must normalize through an importer boundary into the existing semantic document. A third-party decoder model must not escape that boundary. Recognition must not be described as reading support.

## Repository safety

Fork `main` must remain exactly:

`60c2e5de0887b1bcdd426d932632946edd07d3c3`

No pull request, merge, upstream modification, deployment, workflow dispatch, sampled-audio reopening, teacher-mode implementation, or format implementation is authorized by this branch-creation record.

Any new feature work should occur on a feature-specific branch created from this clean branch after a separately bounded plan is approved.

## Required continuity reading

Before using this branch as a parent, read:

1. `AGENTS.md`;
2. `docs/implementation-status.md`;
3. `docs/KNOWN_PROBLEMS_AND_PROVEN_SOLUTIONS.md`;
4. this `BRANCH_AUTHORITY.md`; and
5. the rejected sample branch's `BRANCH_AUTHORITY.md` only when the work concerns playback history or lineage safety.
