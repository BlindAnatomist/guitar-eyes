name: MXL audition first-focus repair gate

on:
  push:
    branches:
      - work/mxl-execution-gate
    paths:
      - .github/workflows/mxl-source-execution-gate.yml

permissions:
  contents: read
  statuses: write

concurrency:
  group: mxl-audition-first-focus-repair-gate
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      SOURCE_SHA: 51741c03a9eaa339940c84d53e0f0f00e6413a93
      STATUS_CONTEXT: guitar-eyes/mxl-audition-first-focus-repair-gate
    steps:
      - name: Mark gate pending
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state: 'pending',
              context: process.env.STATUS_CONTEXT,
              description: 'Running exact first audition focus repair gate',
              target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
            });

      - name: Check out exact repair source
        uses: actions/checkout@v4
        with:
          ref: ${{ env.SOURCE_SHA }}
          fetch-depth: 0
          persist-credentials: false

      - name: Verify exact source and accepted convergence ancestry
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$SOURCE_SHA"
          git merge-base --is-ancestor 7c4ac3d20fbb1d1abc547d30039599804bfdbd7e HEAD
          test -z "$(git status --short)"
          mkdir -p /tmp/mxl-focus-repair-evidence
          {
            echo "source_sha=$(git rev-parse HEAD)"
            echo "trigger_sha=${GITHUB_SHA}"
            echo "accepted_convergence_ancestor=7c4ac3d20fbb1d1abc547d30039599804bfdbd7e"
            echo "runner=${RUNNER_OS}"
          } > /tmp/mxl-focus-repair-evidence/summary.txt

      - name: Set up Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install locked dependencies
        shell: bash
        run: |
          set -o pipefail
          node --version | tee /tmp/mxl-focus-repair-evidence/node-version.txt
          npm --version | tee /tmp/mxl-focus-repair-evidence/npm-version.txt
          npm ci --no-audit --no-fund 2>&1 | tee /tmp/mxl-focus-repair-evidence/npm-ci.log

      - name: Run complete inherited and repair test suite
        shell: bash
        run: |
          set -o pipefail
          CI=true npm test -- --watchAll=false --runInBand 2>&1 | tee /tmp/mxl-focus-repair-evidence/test.log

      - name: Build production application
        shell: bash
        run: |
          set -o pipefail
          CI=false npm run build 2>&1 | tee /tmp/mxl-focus-repair-evidence/build.log

      - name: Inspect compiled repair artifact and repository cleanliness
        shell: bash
        run: |
          set -euo pipefail
          main_js="$(find build/static/js -maxdepth 1 -type f -name 'main.*.js' | head -n 1)"
          test -n "$main_js"
          test -f build/index.html
          grep -Fq '<title>Test build Guitar Eyes first audition focus repair proof 1G</title>' build/index.html
          grep -Fq 'Test build: Guitar Eyes first audition focus repair proof 1G.' build/index.html
          grep -Fq 'Guitar Eyes first audition focus repair proof 1G' "$main_js"
          grep -Fq 'compressed MusicXML tablature' "$main_js"
          grep -Fq 'META-INF/container.xml' "$main_js"
          grep -Fq 'Previous position' "$main_js"
          grep -Fq 'Read current position' "$main_js"
          grep -Fq 'Next position' "$main_js"
          grep -Fq 'Audition current position' "$main_js"
          grep -Fq 'Position navigation' "$main_js"
          grep -Fq 'Position audio' "$main_js"
          grep -Fq 'Current position is a rest. No pitched sound was played.' "$main_js"
          grep -Fq 'iPhone tablature reader' "$main_js"
          grep -Fq 'Desktop tablature reader' "$main_js"
          git diff --exit-code
          git diff --cached --exit-code
          {
            echo "main_js=$main_js"
            sha256sum build/index.html "$main_js"
            echo "compiled_mxl_contract=passed"
            echo "compiled_audition_contract=passed"
            echo "compiled_first_focus_repair_identity=passed"
            echo "tracked_repository_clean=passed"
          } | tee /tmp/mxl-focus-repair-evidence/artifact-inspection.txt

      - name: Upload short-lived verification evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mxl-audition-first-focus-repair-evidence
          path: /tmp/mxl-focus-repair-evidence
          if-no-files-found: error
          retention-days: 1

      - name: Publish final gate status
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const state = '${{ job.status }}' === 'success'
              ? 'success'
              : ('${{ job.status }}' === 'failure' ? 'failure' : 'error');
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state,
              context: process.env.STATUS_CONTEXT,
              description: state === 'success'
                ? 'Exact first audition focus repair gate passed'
                : 'Exact first audition focus repair gate did not pass',
              target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
            });
