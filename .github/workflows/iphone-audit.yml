name: iPhone extension verification

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    name: Verification is temporarily paused during atomic proof assembly
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Restore push verification after the bounded proof files are assembled.'
