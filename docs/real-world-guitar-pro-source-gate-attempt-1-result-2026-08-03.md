# Real-World Guitar Pro Source Gate — Attempt 1 Result

Date: August 3, 2026

Repository: `BlindAnatomist/guitar-eyes`

Feature source tested:

`a5b6dee74fb29778c59e5da62f89d480f3de6667`

Workflow run:

`30853868271`

Job:

`91820177102`

## Result

Failed at the focused test step. The gate stopped before the complete inherited suite, production build, fixture generator, independent cross-format decode audit, or artifact upload.

## Passed evidence

Ten Guitar Pro-focused suites passed:

1. archive-version inspection;
2. source-family inspection;
3. alphaTab intermediate serialization;
4. version-neutral intermediate evidence;
5. source-evidence normalization;
6. decoder integrity;
7. track inventory;
8. reader-document projection;
9. worker client behavior; and
10. format detection.

Focused count before the shell assertion failure:

- 87 tests passed;
- 1 test failed;
- 88 tests total.

## Exact failure

`src/App.test.js` still expected the retired label:

`Test build: Guitar Pro multi-track binary proof 3D`

The application correctly rendered:

`Test build: Real-world Guitar Pro intake foundation 1`

This was a stale shell assertion. It was not a decoder, source-evidence, normalizer, track-selection, reader, build, or fixture failure.

## Repair

Commit `27a8c96314234a6a5dd1606a8336c51f8f4c1870` changed only the stale assertion to match the current authorized foundation label. No application behavior or support claim was changed.

## Rerun discipline

The failed source is not being rerun unchanged. A new gate must target the repaired exact head that includes this result record. The second gate must repeat the focused tests, complete suite, build, artifact inspection, lawful fixture generation, independent alphaTab decode, and one-day artifact upload.

## Repository authority

Fork `main` was restored immediately after the run began. The upstream repository remained untouched. No pull request, merge, Pages publication, or deployment occurred.