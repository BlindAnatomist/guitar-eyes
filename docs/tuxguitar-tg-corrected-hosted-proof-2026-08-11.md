# TuxGuitar `.tg` Corrected Hosted Proof

Date: August 11, 2026

Branch: `work/tuxguitar-tg-intake-investigation`

Corrected source-gated implementation: `b3a8d229aee832a7f6ea994dfc7465ff07d608c3`

Corrected hosted candidate: `dee0ba8d63d22c47b6570778acf8dad7ed003942`

Producer authority: TuxGuitar 2.1.0, tag commit `2c46e2a1cccdfdfa6e6f2692f241bd60bf418129`

Publication run: `31537714794`

Publication job: `93932714111`

Result: passed.

## Publication authority

The hosted publication proved that the candidate descended from the exact corrected source-gated implementation and differed from it only by:

- the corrected source-gate result record;
- the unique corrected hosted identity in `public/index.html`;
- the two matching build-identity regression tests.

No TuxGuitar decoder, normalizer, reader-document, track-inventory, structured-routing, application-routing, or format-detection runtime source changed after the successful corrected source gate.

## Hosted gate

The publication run passed:

- exact candidate checkout;
- exact publication-authority diff;
- locked dependency installation;
- corrected deterministic TuxGuitar fixture verification;
- focused corrected publication tests;
- optimized Pages build;
- production artifact inspection;
- GitHub Pages configuration and upload;
- deployment;
- complete live JavaScript asset-inventory read-back.

The live read-back required the corrected build identity and verified the compiled presence of the legacy and modern TuxGuitar format markers, native modern file-format marker `TuxGuitar_file_format 2.0.0`, and pinned producer commit `2c46e2a1cccdfdfa6e6f2692f241bd60bf418129`.

## Clean-main restoration

The temporary publisher was dispatched from `main` only as the repository's established one-shot Pages transport. Immediately after dispatch, fork `main` was restored to clean authority `60c2e5de0887b1bcdd426d932632946edd07d3c3`.

The corrected candidate remains preserved on the work branch. No merge to `main` occurred.

## Real-iPhone boundary

The user's earlier real-iPhone Safari/VoiceOver test successfully covered source-derived TuxGuitar 1.0, 1.1, 1.2, 1.3, 1.5, and the superseded modern candidate. That evidence remains applicable to the unchanged legacy 1.0, 1.1, 1.2, 1.3, and 1.5 routes.

Because the later producer-source correction materially changed the modern 2.0 interpretation, final acceptance now requires only one bounded real-iPhone Safari/VoiceOver retest of the corrected 2.0 fixture. The user does not need to repeat the five unchanged legacy fixtures.

## Decision

Corrected hosted publication passes. Final TuxGuitar checkpoint closure is pending only the corrected modern 2.0 real-iPhone Safari/VoiceOver acceptance test.
