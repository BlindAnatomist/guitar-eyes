# PowerTab `.pt2` Version-11 Completion Audit and Continuation Ledger

Date: August 5, 2026

Repository: `BlindAnatomist/guitar-eyes`

Status: authoritative continuation ledger. This document records completed evidence, the exact remaining gates, the clean-convergence boundary, and the work that must not be repeated.

## Audited authority

Forensic development and evidence branch:

`work/powertab-pt2-v11-editor-parity`

Exact audited pre-ledger head:

`85593d6c545c37e9adca478462ee198a7afdce99`

Accepted format-only baseline branch:

`work/accepted-format-intake-convergence`

Exact accepted baseline head at audit time:

`e64990ab3e446e5aa1d4eeefbf556a9dc71bd63d`

Accepted application source inherited by both lines:

`2a8e5951ec4ced3ce63b2df85d82e54c0ba79ea0`

Clean fork-main authority:

`60c2e5de0887b1bcdd426d932632946edd07d3c3`

Successful PowerTab parity run:

`31040496589`

Successful parity job:

`92423360890`

Exact tested parity head:

`978b5e364e159fb3113b0986ce24b3f87891db22`

## Audit conclusion

PowerTab producer evidence and development-branch parser parity are complete.

PowerTab is not yet an accepted public format because three bounded gates remain:

1. clean convergence from the accepted format-only baseline;
2. one exact hosted candidate publication with live asset read-back;
3. bounded real-iPhone Safari and VoiceOver acceptance.

The current forensic branch must not itself become the accepted PowerTab line. It is eighty commits beyond the accepted baseline and intentionally preserves the producer failures, corrective runs, temporary mechanisms, and governance repair that explain what happened. That history is valuable evidence, but it is not a clean product lineage.

## Completed evidence that must be reused

The following work is complete and must not be regenerated.

### Exact producer identity

- Power Tab Editor release: `2.0.22`.
- Upstream commit: `13cab27c7127d301f2747671071e53eb203dc940`.
- Official installer SHA-256: `523f12b26b457afa1ea8b15cf0daa2dfd1d82106da723150f662d8bee6a48037`.
- Installed executable SHA-256: `d6bc20f65edbbb509d15cf5e8e10a18ebbf14a48212ea3b417173f9b290046b1`.
- Exact application window title was preserved in the fixture manifest.

### Canonical editor-exported evidence

Canonical binary:

`fixtures/powertab-v11/powertab-v11-editor-export-six-position.pt2`

- compressed bytes: `973`;
- SHA-256: `6494d78b001a43322362d962b21ce88029f3c7c4cf231b9e8da5af2a3bf85835`;
- editor-exported: true;
- internal version: `11`.

Canonical decompressed audit:

`fixtures/powertab-v11/powertab-v11-editor-export-six-position.json`

- bytes: `3651`;
- SHA-256: `c5082b9f7dec3401b74373b037ae7fb438f77e2db86f85c10322f6313d52f5bd`.

The exact editor preserved the authored score and converted twelve semantically empty arrays to `null`. The permanent importer now accepts those lawful empty representations without widening unsupported notation.

### Development-branch verification

Run `31040496589` passed all of these gates:

1. exact branch and accepted-source ancestry;
2. exact changed-file authority;
3. fork-main authority;
4. canonical binary, JSON, and base64-mirror identity;
5. locked dependency installation;
6. 3 focused suites and 23 focused tests;
7. all 53 suites and all 328 tests;
8. optimized production build;
9. bundle inspection;
10. 26 built files totaling `7,021,085` bytes;
11. absence of executable, editor, PowerTab binary, soundfont, and audio assets from the built application;
12. clean tracked checkout;
13. evidence upload;
14. enforced successful checkpoint conclusion.

### Governance reconciliation

The repository now contains the Val's Music Vault-derived operational rules for:

1. the one-failure plus one-corrective-run circuit breaker;
2. fail-forward materialization;
3. transport switching after repeated failure;
4. intact-file handoff;
5. formatter fixed-point preparation;
6. bounded owner-operated actions;
7. preserving temporary evidence only until lasting work is committed.

These rules are mandatory for the remaining PowerTab work.

## Development evidence that must remain forensic

The following records remain valuable on the forensic branch but must not be copied into the clean accepted line as product history:

1. `docs/powertab-pt2-v11-hosted-producer-execution-preflight-2026-08-05.md`;
2. `docs/powertab-pt2-v11-hosted-producer-run-1-failure-2026-08-05.md`;
3. `docs/powertab-pt2-v11-hosted-producer-run-2-failure-2026-08-05.md`;
4. `docs/powertab-pt2-v11-hosted-producer-run-3-timeout-2026-08-05.md`;
5. `docs/powertab-pt2-v11-hosted-producer-run-4-gui-blocker-2026-08-05.md`;
6. `docs/powertab-pt2-v11-hosted-producer-run-5-result-2026-08-05.md`;
7. `docs/powertab-pt2-v11-hosted-producer-run-5-semantic-harness-failure-2026-08-05.md`;
8. superseded producer and parity workflows;
9. producer GUI automation scripts;
10. one-shot trigger files.

Preserving these records does not authorize replaying them.

## Required clean convergence

The next product branch must be:

`work/powertab-pt2-v11-clean-convergence`

It must begin exactly from:

`e64990ab3e446e5aa1d4eeefbf556a9dc71bd63d`

The branch must be assembled through Git object and repository APIs from exact preserved blobs. Do not use a hosted Action to rediscover or reconstruct the source. Do not cherry-pick the eighty-commit forensic history.

The clean branch should contain one intentional PowerTab application commit above the accepted baseline, followed only by bounded documentation or acceptance commits.

## Exact permanent transfer whitelist

Only the following PowerTab implementation, evidence, regression, license, and governance files may differ from the accepted baseline before the candidate-specific build identity is added.

### Governance and provenance

- `AGENTS.md`
- `THIRD_PARTY_NOTICES.md`
- `docs/KNOWN_PROBLEMS_AND_PROVEN_SOLUTIONS.md`
- `docs/cross-repository-execution-governance-reconciliation-2026-08-05.md`
- `docs/known-problems-register-addendum-execution-gates.md`
- `docs/known-problems-register-addendum-powertab-source-evidence.md`
- `docs/powertab-pt2-v11-completion-audit-and-continuation-ledger-2026-08-05.md`

### Reproducible fixture evidence

- `fixtures/powertab-v11/README.md`
- `fixtures/powertab-v11/manifest.json`
- `fixtures/powertab-v11/powertab-v11-editor-export-six-position.comparison.json`
- `fixtures/powertab-v11/powertab-v11-editor-export-six-position.json`
- `fixtures/powertab-v11/powertab-v11-editor-export-six-position.pt2`
- `fixtures/powertab-v11/powertab-v11-editor-export-six-position.pt2.base64`
- `fixtures/powertab-v11/powertab-v11-original-six-position.pt2`
- `fixtures/powertab-v11/powertab-v11-original-six-position.pt2.base64`
- `fixtures/powertab-v11/powertab-v11-original-six-position.source.json`
- `scripts/generate-powertab-v11-proof.mjs`

### Runtime and application integration

- `package.json`
- `src/App.js`
- `src/GuitarProTrackSelector.js`
- `src/powerTabErrors.js`
- `src/powerTabLimits.js`
- `src/powerTabReaderDocuments.js`
- `src/powerTabSourceNormalizer.js`
- `src/powerTabTrackInventory.js`
- `src/powerTabV11Decoder.js`
- `src/powerTabV11PositionParser.js`
- `src/powerTabV11Schema.js`
- `src/powerTabV11SystemParser.js`
- `src/powerTabV11SystemStructure.js`
- `src/structuredTabReaderDocuments.js`
- `src/tabFormatDetector.js`

### Permanent regression coverage

- `src/App.powerTab.test.js`
- `src/App.test.js`
- `src/GuitarProTrackSelector.test.js`
- `src/GuitarProTrackSelector.test.js`
- `src/powerTabReaderDocuments.test.js`
- `src/powerTabSourceNormalizer.test.js`
- `src/powerTabTrackInventory.test.js`
- `src/powerTabV11Decoder.test.js`
- `src/structuredTabReaderDocuments.test.js`
- `src/tabFormatDetector.test.js`

The duplicate appearance of no path is authorized. The convergence implementation must normalize this list before comparing it to the candidate tree.

## Candidate-specific build identity

The clean candidate must update `public/index.html` before hosted verification.

Required identity:

- page title: `Test build Guitar Eyes PowerTab version 11 clean convergence proof`;
- first heading: `Test build: Guitar Eyes PowerTab version 11 clean convergence proof.`;
- description: a bounded statement that this candidate tests verified Power Tab Editor `.pt2` internal version 11 through the shared semantic readers.

The build identity must remain the first level-one heading before the React root and appear exactly once in the accessibility tree.

## One remaining hosted checkpoint

After the clean branch exists, one and only one hosted checkpoint is justified.

That checkpoint must combine clean-convergence verification and publication rather than splitting them into repeated runs.

It must:

1. check out the exact immutable clean-convergence commit;
2. prove ancestry from `e64990ab3e446e5aa1d4eeefbf556a9dc71bd63d`;
3. compare the exact changed-file set with the normalized whitelist plus `public/index.html` and the clean preflight record;
4. prove that no temporary PowerTab workflow, trigger, producer helper, Wine artifact, editor executable, or failure record entered the clean line;
5. verify every committed fixture and mirror hash;
6. verify the manifest support state and editor provenance;
7. install locked dependencies once;
8. run all focused PowerTab, routing, selection, focus, and format-detection suites;
9. run the complete inherited suite once;
10. create the optimized Pages build;
11. inspect the bundle and reject editor, executable, `.pt2`, audio, soundfont, and playback assets;
12. verify the unique static title and first heading in built `index.html`;
13. publish the exact verified artifact through the already proven bounded Pages mechanism;
14. perform live HTML and JavaScript read-back;
15. verify that the hosted asset carries the exact candidate identity and PowerTab route;
16. upload one bounded evidence artifact;
17. restore fork `main` exactly to `60c2e5de0887b1bcdd426d932632946edd07d3c3` immediately after publication;
18. independently confirm that fork `main` is identical, zero ahead, zero behind, and has zero changed files.

The Pages mechanism must follow the proven temporary-main cleanup order. A publication workaround is incomplete until `main` is independently restored.

## Circuit-breaker boundary for the hosted checkpoint

If the hosted checkpoint fails:

1. inspect the exact failed step;
2. classify only the gate that ran;
3. preserve any valid materialized evidence;
4. permit one corrective run only after the defect is fully identified;
5. if the corrective run exposes a second defect, declare the circuit open and stop.

No third exploratory run is authorized.

## Bounded real-iPhone acceptance

John is required only after the hosted candidate and live read-back pass.

The bounded acceptance should use the canonical editor-exported fixture and confirm:

1. the first heading names the PowerTab version-11 clean convergence proof;
2. the `.pt2` file can be selected through the iPhone Files picker;
3. focus returns to the intended result heading;
4. the application identifies the format without overstating compatibility;
5. the supported guitar player inventory is correct;
6. the selected player can be loaded without silent selection;
7. all six semantic positions are reachable in order;
8. the timed rest is announced as a rest;
9. the final two-note chord is announced as a chord;
10. Previous and Next remain quiet;
11. Read current position remains the only full instruction action;
12. ordinary unplayed strings remain omitted;
13. no playback controls or playback language reappear;
14. existing ASCII, MusicXML, MXL, and Guitar Pro routes are not re-tested unless a direct regression appears.

This is a bounded product judgment, not a request for dependency, build, workflow, or repository work from John.

## Post-acceptance closure

Only after real-iPhone acceptance passes may the repository:

1. record the owner's exact observations;
2. change PowerTab `.pt2` internal version 11 from provisional to accepted in `docs/implementation-status.md`;
3. update `AGENTS.md` to list the exact accepted profile;
4. update the manifest support state from parity-verified to accepted only if the acceptance terminology requires a separate state;
5. create the clean convergence result and real-iPhone acceptance records;
6. declare the clean convergence branch the next format-only operational baseline;
7. close PowerTab `.pt2` version 11 before evaluating legacy `.ptb` or another format family.

## Work explicitly prohibited by this ledger

Do not:

1. rerun Power Tab Editor under Wine;
2. regenerate or replace the canonical editor-exported file;
3. repeat producer identity work;
4. repeat the forensic failure sequence;
5. publish the eighty-commit development branch as the accepted line;
6. copy failure records into the clean convergence;
7. add `.ptb`, older `.pt2` internal versions, bass, alternate tunings, or arbitrary PowerTab compatibility;
8. reopen playback, Iowa samples, teacher mode, bookmarks, scoring, or AI instruction;
9. merge to fork `main`;
10. modify upstream;
11. open a pull request;
12. incur paid cost;
13. involve John before the exact hosted candidate is ready.

## Exact next action

Create `work/powertab-pt2-v11-clean-convergence` atomically from `e64990ab3e446e5aa1d4eeefbf556a9dc71bd63d` using the normalized permanent whitelist above, add the candidate-specific `public/index.html` identity and one clean preflight record, and stop for exact-head inspection before any hosted checkpoint is installed or triggered.
