# Historical PowerTab `.pt2` Internal Versions 1–10 Hosted Proof

Date: 2026-08-10

Work branch: `work/powertab-pt2-v1-v10-investigation`

Exact immutable hosted source: `930d00831cb71b4fad6f4771f3009be8cb28670e`

Source-gate run: `31441975991`

Hosted publication run: `31442266091`

Live candidate: `https://blindanatomist.github.io/guitar-eyes/`

This record is documentation-only. It does not yet claim real-device acceptance. Historical `.pt2` internal versions 1 through 10 remain provisional until bounded real-iPhone Safari and VoiceOver testing succeeds.

## Source evidence before publication

The source gate succeeded without a corrective run. It preserved ten deterministic project-authored source-derived `.pt2` fixtures and recorded successful fixture generation, fixture verification, locked dependency installation, focused tests, the complete inherited suite, and production build.

The hosted publication candidate was built from exact source `930d00831cb71b4fad6f4771f3009be8cb28670e` after the source-gate workflow had been removed. The only changes after the successful source gate were the checkpoint-specific static page title and first level-one heading plus their two build-identity tests.

## Hosted publication result

Run `31442266091` completed successfully on its first attempt.

The publication gate verified:

1. the temporary `main` launcher was a one-file child of clean fork-main authority `60c2e5de0887b1bcdd426d932632946edd07d3c3`;
2. the workflow checked out exact work source `930d00831cb71b4fad6f4771f3009be8cb28670e`;
3. the successful source-gate lineage and deterministic fixture materialization were intact;
4. the ten committed `.pt2` fixtures regenerated with no diff;
5. all ten base64 transport twins reproduced their exact binaries;
6. every fixture hash and embedded internal version matched its manifest evidence;
7. both build-identity tests passed;
8. seven focused PowerTab suites passed with 41 tests;
9. the complete inherited suite passed with 63 test suites and 381 tests;
10. the optimized production build compiled successfully;
11. production artifact inspection found no `.pt2`, `.ptb`, Guitar Pro, or other tablature fixture leakage into the build;
12. the exact historical `.pt2` compatibility markers were present in the compiled JavaScript;
13. GitHub Pages deployment succeeded;
14. live HTML contained the exact checkpoint heading;
15. live `asset-manifest.json` identified seven deployed JavaScript files;
16. all seven deployed JavaScript files were fetched and inspected;
17. the live combined JavaScript contained the expected historical serialization, `.pt2` container, v11-preservation, and version-10 milestone markers.

The live read-back reported:

- `live_js_files=7`;
- `historical_pt2_heading=present`;
- `historical_pt2_markers=present`.

## Build identity

The deployed candidate identifies itself before the React root as:

`Test build: Guitar Eyes historical PowerTab PT2 internal versions 1 through 10 proof.`

The HTML title is:

`Test build Guitar Eyes historical PowerTab PT2 internal versions 1 through 10 proof`

## Main cleanup

After the hosted run completed successfully, fork `main` was force-restored to exact clean authority:

`60c2e5de0887b1bcdd426d932632946edd07d3c3`

The temporary publication workflow therefore does not remain on `main` and no compensating cleanup commit was added.

## Remaining acceptance gate

The remaining gate is bounded real-iPhone Safari and VoiceOver testing of the exact hosted candidate using the ten deterministic `.pt2` internal-version fixtures.

The owner should judge only observed behavior. Automated and hosted evidence must not be strengthened into a real-device claim before that test is complete.
