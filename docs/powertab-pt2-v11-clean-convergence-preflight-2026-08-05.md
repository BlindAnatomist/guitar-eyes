# PowerTab `.pt2` Version-11 Clean Convergence Preflight

Date: August 5, 2026

Repository: `BlindAnatomist/guitar-eyes`

Status: clean branch assembly preflight. No hosted checkpoint, publication, pull request, merge, or real-iPhone acceptance is performed by this record.

## Authority

Clean convergence branch:

`work/powertab-pt2-v11-clean-convergence`

Exact parent and accepted baseline head:

`e64990ab3e446e5aa1d4eeefbf556a9dc71bd63d`

Accepted application source inherited from that baseline:

`2a8e5951ec4ced3ce63b2df85d82e54c0ba79ea0`

Forensic development branch:

`work/powertab-pt2-v11-editor-parity`

Exact forensic source head used only for preserved blobs and governance evidence:

`7a9bf82ac82230169bf6233f8693965429b3c618`

Exact previously tested parity head:

`978b5e364e159fb3113b0986ce24b3f87891db22`

Successful parity run and job:

- run `31040496589`;
- job `92423360890`.

Clean fork-main authority:

`60c2e5de0887b1bcdd426d932632946edd07d3c3`

## Assembly method

The clean tree is assembled atomically through Git object APIs from the accepted baseline tree and exact already committed blobs.

It does not cherry-pick or replay the forensic branch history. It does not reconstruct binary evidence through text transport. It does not invoke Power Tab Editor, Wine, dependency installation, tests, builds, or GitHub Actions.

The assembly transfers exactly the unique permanent whitelist recorded in:

`docs/powertab-pt2-v11-completion-audit-and-continuation-ledger-2026-08-05.md`

The transfer consists of:

1. seven governance and provenance files;
2. nine PowerTab fixture files;
3. one deterministic fixture generator;
4. fifteen runtime and integration files including `package.json`;
5. nine permanent regression files;
6. this preflight record;
7. the candidate-specific `public/index.html` identity.

`package-lock.json` remains inherited unchanged from the accepted baseline.

## Preserved completed evidence

The clean line reuses, without regeneration:

- the canonical 973-byte editor-exported `.pt2` binary;
- SHA-256 `6494d78b001a43322362d962b21ce88029f3c7c4cf231b9e8da5af2a3bf85835`;
- the canonical 3651-byte decompressed audit;
- SHA-256 `c5082b9f7dec3401b74373b037ae7fb438f77e2db86f85c10322f6313d52f5bd`;
- exact Power Tab Editor 2.0.22 provenance;
- parser parity;
- 3 focused suites and 23 focused tests;
- all 53 suites and all 328 tests;
- the optimized build and bundle inspection already recorded by the successful parity run.

These development-branch results justify convergence. They do not replace the one remaining clean-line verification and publication checkpoint.

## Explicit exclusions

The clean tree excludes:

1. every producer failure and timeout record;
2. every superseded or temporary PowerTab workflow;
3. every trigger file;
4. producer GUI automation and Wine helpers;
5. installer and executable artifacts;
6. temporary semantic harnesses;
7. playback, Iowa samples, teacher mode, bookmarks, scoring, and AI work;
8. legacy `.ptb`, older `.pt2` versions, bass, alternate tunings, and arbitrary compatibility claims.

## Candidate identity

The clean candidate uses:

- title: `Test build Guitar Eyes PowerTab version 11 clean convergence proof`;
- first heading: `Test build: Guitar Eyes PowerTab version 11 clean convergence proof.`;
- bounded description naming verified Power Tab Editor `.pt2` internal version 11 through the shared semantic readers.

The first heading remains before the React root and appears once in the static document.

## Stopping point

After atomic branch creation, perform only read-only inspection of:

1. exact branch parentage;
2. one-commit clean lineage;
3. exact changed-file boundary;
4. absence of temporary or forensic files;
5. candidate identity;
6. fork-main authority;
7. queued and active workflow state.

Do not install or trigger the hosted checkpoint until that exact-head inspection is complete and separately reported.
