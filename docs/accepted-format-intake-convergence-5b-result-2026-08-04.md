# Accepted Format-Intake Convergence 5B Result

Date: 2026-08-04

## Authority

- Branch: `work/accepted-format-intake-convergence`
- Clean accepted 4C base: `030e1f6af2de23e41ad993ab0292893b072664eb`
- Clean convergence application source: `2a8e5951ec4ced3ce63b2df85d82e54c0ba79ea0`
- Preserved Guitar Pro development and evidence branch: `work/real-world-guitar-pro-intake`
- Accepted Guitar Pro closure head: `f121265276c314524083800970d9074b80cd1b35`
- Successful convergence run: `30927079592`
- Successful convergence job: `92052050062`
- Successful hosted publication run: `30927636526`
- Successful hosted publication job: `92053971002`
- Clean fork `main` authority: `60c2e5de0887b1bcdd426d932632946edd07d3c3`

## Clean convergence result

The accepted Guitar Pro result was reconstructed as one clean commit above the accepted 4C application source. The convergence transferred only accepted runtime, lawful fixtures and provenance, permanent regression tests, the behavior-preserving compressed-MusicXML lint repair, and final acceptance records.

It excluded temporary workflows, failed and superseded generator records, generator scripts, sampled-audio work, playback reopening, teacher mode, and additional formats.

The convergence gate passed:

- exact whitelist assembly;
- preserved hashes for the five Guitar Pro binaries and generator patch;
- 10 focused suites and 53 focused tests;
- all 47 suites and all 302 tests;
- production build;
- lazy alphaTab bundle boundary;
- format-only reader surface;
- absence of soundfont, synth, audio-worklet, and Iowa sample assets;
- atomic creation of `work/accepted-format-intake-convergence`.

## Hosted 5B result

The clean convergence source was published as:

`Test build: Guitar Eyes format-only clean format-intake convergence proof 5B.`

The corrected publication gate passed:

- exact immutable source and one-commit ancestry;
- proof that no workflow file differed from the accepted 4C base;
- focused identity, format-only, Guitar Pro corpus, GP7 evidence, and application tests;
- production Pages build;
- artifact inspection;
- GitHub Pages configuration, upload, and deployment;
- live hosted HTML and JavaScript read-back.

The first publication attempt did not deploy. It failed only because its authority check incorrectly required the inherited `.github/workflows` directory not to exist. The corrected gate instead proved that the convergence introduced no workflow-file differences.

## Real-iPhone acceptance

On August 4, 2026, John Washburn tested all five project-authored Guitar Pro fixtures on the clean convergence proof with iPhone Safari and VoiceOver.

He traversed all six semantic positions in every file and reported that:

- each file loaded;
- Guitar Eyes recognized and named the corresponding Guitar Pro version while loading;
- all six positions in every file read correctly;
- everything appeared to work correctly in the bounded test.

The exact acceptance record is:

- `docs/accepted-format-intake-convergence-5b-real-iphone-acceptance-2026-08-04.md`.

## Closure

Accepted Format-Intake Convergence 5B is passed and closed.

The clean convergence is now the authoritative format-only baseline for future format investigation.

No merge to `main`, playback reopening, teacher-mode work, upstream modification, or additional format implementation was performed or authorized by this result.
